 * dry runs for locking functions

 * also allow asynchronously prefetching a page, synchronous logic is already built, we need to just make it async, by submitting the necessary params and the function to the bufferpool's thread pool
