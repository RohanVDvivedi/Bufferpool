#ifndef PAGE_FETCHER_H
#define PAGE_FETCHER_H

// page fetcher is responsible for fetching/reading new pages from the disk
// it picks least recently used clean pages from the the queue of clean pages
// and writes them with new data from the disk

#endif