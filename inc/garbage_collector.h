#ifndef GARBAGE_COLLECTOR_H
#define GARBAGE_COLLECTOR_H

// Garbage collector is responsible for writing dirty pages to disk
// it dequeus least recently used dirty pages from the queue of dirty pages 
// and writes their content them to the disk

// least recently used high priority dirty pages will be written to disk first

#endif